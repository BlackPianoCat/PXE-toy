---
# Main entry point for PXE server role
# Orchestrates PXE server setup and Debian image bootstrap

# 1. PXE server setup (TFTP, dnsmasq, nginx, etc.)
- import_tasks: setup.yml

# 2. Bootstrap Debian rootfs for PXE clients
- import_tasks: debootstrap.yml

# 3. Network-setup
- import_tasks: network_setup.yml

---
# Main entry point for PXE server role
# Orchestrates updates, programs, users, and PXE setup

# 0. PXE, Clonezilla, TFTP, nginx setup
- import_tasks: setup.yml

# 1. Update and upgrade system
- import_tasks: updates.yml

# 2. Install programs
- import_tasks: programs.yml

# 3. Create users and configure sudo
- import_tasks: users.yml
